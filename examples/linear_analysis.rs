#![allow(
    clippy::all,
    dead_code,
    unused_imports,
    unused_variables,
    unused_assignments
)]
use rand::{Rng, SeedableRng};
use sha3::digest::{ExtendableOutput, Update};
use shake::Shake256;

fn derive_round_keys(key: &[u8; 32], rounds: usize) -> Vec<u64> {
    const DOMAIN: &[u8] = b"HERRINGFISH-FEISTEL-KEY";
    let mut hasher = Shake256::default();
    hasher.update(DOMAIN);
    hasher.update(key);
    let mut out = vec![0u8; rounds * 8];
    hasher.finalize_xof_into(&mut out);
    (0..rounds)
        .map(|i| {
            let mut b = [0u8; 8];
            b.copy_from_slice(&out[i * 8..i * 8 + 8]);
            u64::from_le_bytes(b)
        })
        .collect()
}

const SBOX: [u8; 256] = [
    ,
];

fn f_function(x: u64, k: u64) -> u64 {
    let mut t = 0u64;
    for i in 0..8 {
        let x_byte = ((x >> (8 * i)) & 0xff) as u8;
        let k_byte = ((k >> (8 * i)) & 0xff) as u8;
        let sb = SBOX[(x_byte ^ k_byte) as usize];
        t |= (sb as u64) << (8 * i);
    }
    let mut bytes = [0u8; 8];
    for i in 0..8 {
        bytes[i] = ((t >> (8 * i)) & 0xff) as u8;
    }
    let mut out_bytes = [0u8; 8];
    for i in 0..8 {
        out_bytes[i] = bytes[i] ^ bytes[(i + 1) % 8] ^ bytes[(i + 3) % 8];
    }
    let mut out = 0u64;
    for i in 0..8 {
        out |= (out_bytes[i] as u64) << (8 * i);
    }
    out
}

fn feistel_encrypt(key: &[u8; 32], pt: &[u8; 16], rounds: usize) -> [u8; 16] {
    let keys = derive_round_keys(key, rounds);
    let mut left = u64::from_le_bytes(pt[0..8].try_into().unwrap());
    let mut right = u64::from_le_bytes(pt[8..16].try_into().unwrap());
    for i in 0..rounds {
        let f_out = f_function(right, keys[i]);
        let new_right = left ^ f_out;
        left = right;
        right = new_right;
    }
    let mut out = [0u8; 16];
    out[0..8].copy_from_slice(&left.to_le_bytes());
    out[8..16].copy_from_slice(&right.to_le_bytes());
    out
}

fn dot_product(a: u128, b: u128) -> u8 {
    ((a & b).count_ones() & 1) as u8
}

fn linear_bias(rounds: usize, samples: usize, trials: usize) -> f64 {
    let key = [0u8; 32];
    let mut rng = rand::rngs::StdRng::seed_from_u64(0x87654321);
    let mut max_bias = 0.0;
    for _ in 0..trials {
        let alpha = ((rng.next_u64() as u128) << 64) | rng.next_u64() as u128;
        let beta = ((rng.next_u64() as u128) << 64) | rng.next_u64() as u128;
        let mut count = 0usize;
        for _ in 0..samples {
            let mut pt = [0u8; 16];
            rng.fill_bytes(&mut pt);
            let ct = feistel_encrypt(&key, &pt, rounds);
            let mut pt_bits = 0u128;
            for i in 0..16 {
                pt_bits |= (pt[i] as u128) << (8 * i);
            }
            let mut ct_bits = 0u128;
            for i in 0..16 {
                ct_bits |= (ct[i] as u128) << (8 * i);
            }
            let a_dot_p = dot_product(alpha, pt_bits);
            let b_dot_c = dot_product(beta, ct_bits);
            if a_dot_p == b_dot_c {
                count += 1;
            }
        }
        let p = count as f64 / samples as f64;
        let bias = (p - 0.5).abs();
        if bias > max_bias {
            max_bias = bias;
        }
    }
    max_bias
}

fn main() {
    let samples = 20000;
    let trials = 20;
    println!(
        "Linear bias sampling, samples={}, trials={}",
        samples, trials
    );
    for rounds in [4, 6, 8] {
        let bias = linear_bias(rounds, samples, trials);
        println!("Rounds {}: max observed bias ≈ {:.6}", rounds, bias);
    }
}
